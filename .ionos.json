{
  "buildpack": "node",
  "build": {
    "engine": "npm",
    "commands": [
      "npm ci",
      "npm run build"
    ]
  },
  "distDir": "dist",
  "documentRoot": "dist",
  "env": {
    "NODE_ENV": "production"
  }
}
