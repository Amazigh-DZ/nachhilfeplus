{
  "name": "nachhilfe-plus",
  "buildpack": "node",
  "build": {
    "engine": "npm",
    "commands": [
      "npm install",
      "npm run build"
    ]
  },
  "outputDir": "dist",
  "env": {
    "NODE_ENV": "production"
  }
}
